/**
 * Provides the redux state manager. The redux state manager exposes the same
 * functions as before for convenience, but those interface with the redux store.
 * InputField will then connect() to the appropriate part of the redux store.
 *
 * Maybe look at how the current forms library handles this.
 */
